import React from 'react';
import { CurrentList } from 'containers'
import { Screen } from 'components'

export default function CurrentListScreen({navigation}) {
  return (
    <Screen>
      <CurrentList />
    </Screen>
  );
}
