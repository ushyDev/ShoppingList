import React from 'react';
import { ArchivedList } from 'containers'
import { Screen } from 'components'

export default function ArchivedListScreen({navigation}) {
  return (
    <Screen>
      <ArchivedList />
    </Screen>
  );
}
